# Plan 0120 | Cross-Service Follow Runtime Closeout

State: OPEN
Lane: P39
Work item: WI-005
Branch: feat/cross-service-follow-runtime-closeout-v1
Target: main
Integration: merge
Roadmap: P39
Plan version: 1
Date: 2026-09-14
Execution owner: unassigned
Coordination owner: /root
Requested model route: gpt-6-astra, high reasoning
Effective runtime model: unknown until reported

## Objective

Close WI-005 provider-free by proving the integrated cross-service follow
product through a reproducible packaged cache-only runtime, fresh HTTP/CLI/MCP
clients, restart durability, exact authorization, truthful readiness and
owned-process teardown.

## Current State

Plans 0115 and 0118 are closed and WI-001/WI-002/WI-004 are `DONE`. Native
fixture execution is proven; only stock packaged-runtime readback, restart,
denial, identity and teardown acceptance remains for WI-005.

## Definition Of Done

Every acceptance criterion passes at one reviewed joined checkpoint, the exact
artifact/runtime receipt is retained, canonical integration is recorded, and
WI-005 is reconciled to `DONE` without widening external-effect authority.

## Scope

- add a repo-only follow dogfood probe and focused adversarial harness tests;
- prepare synthetic Reddit community/user, YouTube channel, X history,
  general-overlap, quarantine and unauthorized-partition evidence through the
  real coordinator, job runner, injected native routes and publisher;
- run the stock packaged cache-only service for two start/stop cycles and prove
  catalog, lifecycle/history, provenance, filter and denial parity;
- retain exact source, artifact, manifest/catalog/schema, process, database
  mutation and cleanup receipts.

## Non-Goals

No live locator, provider/network/yt-dlp execution, browser, credential,
installed runtime/database, real schedule, lifecycle-policy relaxation,
delivery, release/deployment or issue mutation. The cache-only outer runtime
denial does not replace Plan 0118's inner fixture execution proof.

## Acceptance Criteria

1. Two clean-source builds are byte-identical and all runtime/client identities
   match the artifact.
2. HTTP, Python and MCP capability catalogs agree while declared support,
   dependency readiness and execution readiness remain distinct.
3. Authorized reads preserve exact target IDs, revisions, frozen run context,
   receipts, deduplicated versions and every collection sighting.
4. Cross-profile list/get/search reveal no target, history, count or provenance.
5. All runtime mutation/run/resume attempts fail with the expected cache-only
   denial and leave corpus, acquisition and schedule ledgers unchanged.
6. A fresh second MCP process replays identical durable evidence after restart.
7. Both controller cycles stop cleanly; socket and owned-process census end
   empty. Focused/full Python, Go/vet, package, audit and diff checks pass.

## Ownership And Topology

One top-level owner, no children, at most two implementation attempts, one
independent joined review and one closed-world remediation. Lane writes are
`dev/last30days/scripts/follow_dogfood.py`, `tests/test_follow_dogfood.py`, one
retained receipt, and this plan. Existing Packet 2 fixtures may be read.
Coordinator owns shared app/client/HTTP/CLI/MCP/contracts, versions/manifests,
product docs and authority projections; change them only for a reproduced
public-boundary defect after reconciliation.

## Stop Rules

Stop on ambiguous runtime custody, source/artifact mismatch, unexpected writes,
partition leakage, changed X identity, generic-search fallback, readiness
overclaim, surviving owned processes, shared-file conflict or any excluded
effect. One acceptance run contains two deliberate cycles and is not retry
authority.

## Next Action

Integrate this registration, publish exact isolated custody, then assign one
implementation owner. Successful reviewed canonical integration may move
WI-005 to `DONE`.

### Checkpoint P0120-C01 | 2026-09-14

Plan version: 1

State transition: `unplanned -> PLANNED`.

Progress classification: `blocker_reduction`; the final provider-free runtime
gate is bounded without weakening cache-only policy.

Authority classification: `inherited_authority` for registration and later
provider-free work; all Non-Goals remain `human_gate` effects.

Subagent status and reconciliation: `joined`; one read-only planner completed.
Implementation owner remains unassigned pending exact custody.

Next action: integrate registration and publish a plan-only activation ref.

### Checkpoint P0120-C02 | 2026-09-14

Plan version: 1

State transition: `PLANNED -> OPEN`.

Progress classification: `implementation_ready`; exact isolated custody is
accepted from canonical registration merge `e0fab676`.

Authority classification:

- `inherited_authority` covers the registered provider-free write set only;
- every external-effect gate remains held.

Subagent status and reconciliation: `assigned`; `/root/wave4_wi005_plan`, one
owner, no children. Implementation waits for activation reconciliation.

Next action: publish this plan-only checkpoint and return exact custody.
